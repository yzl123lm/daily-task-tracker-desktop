node_modules/

.env
.env.*
!.env.example

# Secrets and credentials
*.pem
*.key
credentials.json
**/secrets/**

# Build outputs
dist/
dist-alt/
dist-ship*/
*.exe
*.blockmap
release-history.jsonl

# Local ASAR / ship debug extracts (large, regeneratable)
_asar_*/

# Iteration archives (installers); see npm run ship:latest-client
最新客户端/
最新客户端目录/

# Temporary OCR / debug artifacts
_tmp_*
*.traineddata

# Local sample documents (not part of source)
/*.docx

# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo

*.log

# Local Python env (icon generation scripts)
.venv/
__pycache__/

# Auto-generated code knowledge graph (rebuild via graphify)
graphify-out/

# Cursor: synced third-party skills (use npm run impeccable:sync-skills locally)
.cursor/skills/
.cursor/debug/

# Local agent / IDE hooks (optional per machine)
.codex/

# Temporary Impeccable source extracts (skills live under .cursor/skills when synced)
vendor/impeccable-main/
vendor/taste-skill-main/
vendor/ui-ux-pro-max-skill-main/
scripts/impeccable-sync-state.json

# Temporary docx export scratch (scripts/export-*-docx.js)
scripts/_docx_tmp/

# Icon debug artifacts (regenerated by scripts/generate-app-icon.py)
build/ico-dump/
build/debug-*.png
build/brand-reference.png
build/icon-source.png
