项目工作区 Codex 风格页面骨架布局修复整改开发文档｜供 Cursor 分阶段执行适用项目：Electron + 原生 HTML/CSS/JS 工作台｜目标：从居中长卡片改造为浅蓝白 Codex 多栏开发工作台文档用途• 让 Cursor 按步骤排查、修复、验证项目工作区页面骨架。• 避免继续做“局部美化”，重点完成全屏容器、多栏 Grid、面板独立滚动、底部终端抽屉。• 每一阶段均包含目标、涉及文件、修改点、验收标准与回滚提醒。当前问题截图与目标样例对照左图为当前问题：中间大白色长卡片 + 内部滚动。右图为目标方向：浅蓝白多栏开发工作台。当前问题效果目标参考效果说明：本文档以“布局骨架”作为第一优先级，视觉细节在骨架稳定后再统一调优。目录1. 诊断结论与整改目标2. 目标页面骨架定义3. 修复总路线与执行顺序4. 阶段 0：修复前准备与基线记录5. 阶段 1：解除旧样式冲突与居中限宽6. 阶段 2：修复项目工作区根容器7. 阶段 3：两栏 Flex 改造为 Codex Grid8. 阶段 4：项目与任务侧栏独立化9. 阶段 5：Agent 面板结构与滚动修复10. 阶段 6：代码 Diff 审查区重组11. 阶段 7：底部 TerminalDrawer 固定化12. 阶段 8：响应式断点与宽屏适配13. 阶段 9：空任务状态与数据加载验证14. 阶段 10：验收测试与回归检查15. Cursor 分批执行提示词16. 附录：关键 CSS / DOM 参考片段1. 诊断结论与整改目标根据现有页面骨架排查报告，当前页面已经进入 projectWorkspaceLayout.js 的 v3 Layout，但仍嵌在 AI 面板 #panel-ai 内；LeftRail / 项目列表与 #wbProjectWorkspace 分属不同 DOM subtree，尚未合成目标的多栏工作台结构。核心判断：当前属于“部分骨架升级 + 大量视觉美化”，还没有完成 Codex 级页面骨架重构。最关键的阻塞点• ui-nebula-theme.css 对 #wbProjectWorkspace 设置 max-width: 960px 与 margin: 0 auto，直接造成居中大白卡。• project-workspace-codex.css 仍有 max-width: min(1400px, calc(100% - 16px)) 与 margin: 0 auto。• workbench-dev.css 的旧 .wb-project-workspace 规则仍影响 padding、background、overflow。• 主骨架是两栏 Flex，不是目标的项目侧栏 + Agent 区 + Code 区 + 底部终端 Grid。• 滚动区域在 agent-col 整列，未做到主页面不滚动、面板内部独立滚动。1.1 整改总目标项目工作区不再以 AI 面板内的居中卡片展示，而是占满可用工作区域。桌面端改为浅蓝白 Codex 风格多栏开发工作台。保留现有业务功能：项目选择、任务、Agent Timeline、受控写入、代码面板、Diff 审查、终端、工具调用、测试输出、Git 状态。先完成骨架，再做视觉。不要继续在旧单列页面上做局部样式优化。整改对象当前问题目标状态优先级#wbProjectWorkspace被 max-width 与 margin: auto 限制width/height 100%，max-width noneP0.wb-pws-layoutFlex 两栏Grid 三栏 + 底部终端行P0项目任务侧栏项目在旧 nav，任务在 Agent 列独立 wb-pws-project-colP1Agent 列整列 overflow:autoheader / scroll / composer 分区P1Code 区代码、测试、Shell 混在纵向流Diff 审查为主，Shell 移至底部P1响应式>1024 两栏且仍限宽>=1280 多栏，<1024 抽屉/单列P22. 目标页面骨架定义考虑当前项目外层已存在 jl-workbench-nav，本轮修复先将 #wbProjectWorkspace 内部改造成 3 栏 + 底部终端。后续可进一步把 64px LeftRail 纳入统一 Grid。目标桌面结构（本轮落地版）：┌─────────────────────────────────────────────────────────────┐│ TopBar：项目工作区 / 当前项目 / 模式 / 环境 / 新建任务          │├──────────────┬────────────────────┬─────────────────────────┤│ Project Side │ Agent Task Panel   │ Code Review / Diff Panel ││ 项目与任务    │ 需求、Timeline、审批 │ 文件、变更、Diff、测试状态     │├──────────────┴────────────────────┴─────────────────────────┤│ Terminal Drawer：终端 / 工具调用 / 测试输出 / 执行日志          │└─────────────────────────────────────────────────────────────┘区域Grid Area建议尺寸核心内容滚动策略顶部状态栏topbar48px项目名、状态、执行模式、环境、模型、新建任务不滚动项目任务栏project300px当前项目卡片、任务筛选、任务列表、会话记录入口列表内部滚动Agent 执行区agent360px用户需求、执行 Timeline、计划、待审批、输入框中间内容滚动，输入固定代码审查区code剩余宽度代码变更、文件树、Diff、测试状态、操作栏代码主体滚动底部终端terminal220px终端、工具调用、测试输出、执行日志body 内部滚动2.1 目标 DOM 结构<div id="wbProjectWorkspace" class="wb-project-workspace wb-project-workspace--codex">  <div class="wb-pws-layout">    <header class="wb-pws-topbar">...</header>    <aside class="wb-pws-project-col">...</aside>    <section class="wb-pws-agent-col">...</section>    <section class="wb-pws-code-col">...</section>    <footer id="wbPwsTerminalDrawer" class="wb-pws-terminal-drawer">...</footer>  </div></div>3. 修复总路线与执行顺序请 Cursor 必须按阶段执行，每个阶段完成后先截图和自检，避免一次性大改导致定位困难。阶段目标主要文件完成标志0准备分支、记录基线git / 截图有回滚点和当前截图1解除 CSS 冲突ui-nebula-theme.css / project-workspace-codex.css / workbench-dev.css居中卡片消失2根容器全屏app.js / index.html / CSS#wbProjectWorkspace 填满 #panel-ai3Flex 改 GridprojectWorkspaceLayout.js / project-workspace-codex.css出现三栏 + 底部终端4独立项目任务栏projectWorkspaceLayout.js / projectWorkspace.js项目/任务从 Agent 列拆出5Agent 区滚动修复projectWorkspaceLayout.js / CSS输入框固定，Timeline 内部滚动6Code 区重组projectCodePanel.js / diffReviewPanel.js / CSSDiff 区占据右侧主区域7Terminal 固定化TerminalDrawer / CSS终端固定在底部 Grid 行8响应式CSS media query1280/1440/1536 均为桌面布局9空态验证projectWorkspace.js无任务也保留骨架10验收回归全局截图与目标样例接近，功能不丢4. 阶段 0：修复前准备与基线记录本阶段不改代码，只建立可回滚环境和问题基线。创建 Git 分支：fix/project-workspace-codex-layout。保存当前问题截图，至少包含 1280px、1440px、1536px 三个窗口宽度。记录当前 DOM 层级：.jl-app-shell → .jl-main-workspace → #panel-ai → #wbProjectWorkspace。记录当前 #wbProjectWorkspace 计算样式：width、height、max-width、margin、overflow、border-radius、box-shadow。记录当前 .wb-pws-layout 的 display、flex/grid、columns、rows。阶段 0 验收• 具备可回滚分支。• 具备当前问题截图与 computed style 记录。• 未修改任何业务代码。5. 阶段 1：解除旧样式冲突与居中限宽这是 P0 阶段。只要 max-width / margin:auto 没清掉，后续任何 Grid 都会被压成居中卡片。5.1 必查冲突文件文件需要定位的旧规则处理方式ui-nebula-theme.cssbody.jl-project-workspace-active #panel-ai #wbProjectWorkspace max-width:960px margin:0 auto border-radius box-shadow在项目工作区激活态下覆盖为 none / 0，或删除该分支规则project-workspace-codex.css.wb-project-workspace--codex max-width:min(1400px...) margin:0 auto改为 width:100%; height:100%; max-width:none; margin:0workbench-dev.css.wb-project-workspace padding background:#fff overflow:auto限定 legacy 作用域，避免影响 .wb-project-workspace--codex5.2 建议补丁body.jl-project-workspace-active #panel-ai {  width: 100%;  height: 100%;  min-width: 0;  min-height: 0;  overflow: hidden;  padding: 0;}body.jl-project-workspace-active #panel-ai #wbProjectWorkspace,body.jl-project-workspace-active #panel-ai .wb-project-workspace--codex {  width: 100% !important;  height: 100% !important;  max-width: none !important;  margin: 0 !important;  padding: 0 !important;  border-radius: 0 !important;  box-shadow: none !important;  overflow: hidden !important;  background: transparent !important;}/* legacy 旧规则不得影响新版 Codex 工作区 */.wb-project-workspace:not(.wb-project-workspace--codex) {  /* 保留旧页面样式 */}阶段 1 验收• #wbProjectWorkspace 的 computed max-width 为 none。• margin-left / margin-right 不再是 auto。• 页面中间大白卡视觉消失。• 仍可进入项目工作区，功能按钮不报错。6. 阶段 2：修复项目工作区根容器本阶段目标是让工作区成为 #panel-ai 内的全尺寸 canvas，而不是普通内容容器。html,body,.jl-app-shell,.jl-main-workspace,#panel-ai,#wbProjectWorkspace,.wb-pws-layout {  min-width: 0;  min-height: 0;}body.jl-project-workspace-active #panel-ai {  display: block;  overflow: hidden;}body.jl-project-workspace-active #wbProjectWorkspace {  display: block;  position: relative;  inset: auto;}检查项正确结果错误表现#panel-ai100% 宽高、无 padding、overflow hidden内部白卡左右留大边距#wbProjectWorkspace填满 #panel-aicomputed width 小于父容器body/html不出现页面级滚动条页面外层滚动背景浅蓝白背景延续纯白大卡片背景覆盖全部阶段 2 验收• 打开项目后，#wbProjectWorkspace 与 #panel-ai 宽高一致。• 窗口缩放时工作区跟随填满。• 没有整体页面滚动条。7. 阶段 3：两栏 Flex 改造为 Codex Grid当前报告显示主骨架是 Flex 两栏。本阶段将 .wb-pws-layout 改成 Grid。7.1 CSS Grid 核心补丁.wb-pws-layout {  display: grid;  width: 100%;  height: 100%;  min-width: 0;  min-height: 0;  overflow: hidden;  grid-template-columns: 300px 360px minmax(0, 1fr);  grid-template-rows: 48px minmax(0, 1fr) 220px;  grid-template-areas:    "topbar topbar topbar"    "project agent code"    "terminal terminal terminal";  gap: 0;  background: linear-gradient(135deg, #EEF6FF 0%, #F7FBFF 48%, #EAF4FF 100%);}.wb-pws-topbar { grid-area: topbar; }.wb-pws-project-col { grid-area: project; }.wb-pws-agent-col { grid-area: agent; }.wb-pws-code-col { grid-area: code; }.wb-pws-terminal-drawer { grid-area: terminal; }7.2 projectWorkspaceLayout.js 调整方向在 ensureProjectWorkspaceLayout() 内确认生成上述五个区域。不要继续仅依赖 .wb-pws-main flex 容器承载 agent/code。// 伪代码：保持现有 id 和业务挂载点，调整外层骨架workspace.innerHTML = `  <div class="wb-pws-layout">    <header class="wb-pws-topbar" id="wbPwsTopbar"></header>    <aside class="wb-pws-project-col" id="wbPwsProjectCol"></aside>    <section class="wb-pws-agent-col" id="wbPwsAgentCol"></section>    <section class="wb-pws-code-col" id="wbPwsCodeCol">      <div id="wbPwsCodeMount"></div>    </section>    <footer class="wb-pws-terminal-drawer" id="wbPwsTerminalDrawer"></footer>  </div>`;阶段 3 验收• .wb-pws-layout computed display 为 grid。• grid-template-columns 为 300px 360px minmax(0, 1fr) 或等价实现。• 桌面端可看到项目栏、Agent 栏、代码栏、底部终端行。8. 阶段 4：项目与任务侧栏独立化当前项目在 jlProjectList，任务在 wb-pws-agent-col。目标是项目与任务侧栏独立，避免 Agent 列承担太多内容。模块保留/迁移策略说明当前项目卡片复制渲染到 wb-pws-project-col显示项目名、ID、状态、分支、环境任务筛选新增在 wb-pws-project-col全部 / 进行中 / 等待审批 / 已完成任务列表从 Agent 列拆出或新增专用渲染卡片显示标题、文件路径、状态、更新时间会话记录入口保留在底部只作为入口，不混用项目上下文旧 jlProjectList暂不删除避免破坏全局导航与选择逻辑.wb-pws-project-col {  min-width: 0;  min-height: 0;  overflow: hidden;  display: flex;  flex-direction: column;  border-right: 1px solid #DCEBFA;  background: rgba(255,255,255,.72);  backdrop-filter: blur(18px);}.wb-pws-project-list,.wb-pws-task-list {  flex: 1;  min-height: 0;  overflow-y: auto;}阶段 4 验收• 任务列表不再挤占 Agent Timeline 空间。• 项目与任务侧栏内部可独立滚动。• 点击任务后 Agent 区和 Code 区能同步切换。9. 阶段 5：Agent 面板结构与滚动修复当前 wb-pws-agent-col 整列 overflow:auto，会导致输入框、Timeline、计划方案一起滚动。目标是固定 header 和 composer，内容区独立滚动。.wb-pws-agent-col {  min-width: 0;  min-height: 0;  overflow: hidden;  display: flex;  flex-direction: column;  border-right: 1px solid #DCEBFA;  background: rgba(255,255,255,.82);}.wb-pws-agent-header {  flex: 0 0 auto;  min-height: 52px;}.wb-pws-agent-scroll {  flex: 1 1 auto;  min-height: 0;  overflow-y: auto;  padding: 12px;}.wb-pws-agent-composer {  flex: 0 0 auto;  border-top: 1px solid #DCEBFA;  background: rgba(255,255,255,.92);}Agent 子区展示内容是否固定Header任务标题、状态、更新时间、更多操作固定顶部Scroll Body用户需求、Agent Timeline、计划方案、待审批变更内部滚动Composer下一步追问、@Agent、附件、知识库、发送按钮固定底部阶段 5 验收• Agent 列不再整体滚动。• 输入框始终固定在 Agent 区底部。• Timeline 长内容只在中间内容区滚动。10. 阶段 6：代码 Diff 审查区重组目标是右侧代码区域以 Diff 审查为主，不再把 Shell、测试、Git 等全部纵向堆在一个内容流中。Code 区子模块目标内容处理建议Tabs代码变更 / 文件树 / 测试状态 / 运行记录固定在顶部变更概览变更文件数、+/- 行数、全部接受、逐个接受、全部拒绝置于 Diff 上方文件/变更列表文件名、路径、增删行、状态可放在 Code 区左侧或顶部卡片Diff 主体左右对比或统一视图，红删绿增占据主空间，内部滚动操作栏拒绝、评论、接受变更固定底部.wb-pws-code-col {  min-width: 0;  min-height: 0;  overflow: hidden;  display: flex;  flex-direction: column;  background: rgba(255,255,255,.9);}.wb-pws-code-tabs,.wb-pws-code-actions {  flex: 0 0 auto;}.wb-pws-code-body {  flex: 1 1 auto;  min-height: 0;  overflow: auto;}阶段 6 验收• 右侧区域主视觉为代码变更/Diff，而不是一堆纵向卡片。• Diff 内容可独立滚动。• 接受/拒绝变更操作固定且清晰。11. 阶段 7：底部 TerminalDrawer 固定化报告显示 TerminalDrawer 基本符合，但需要确保它是 Grid 第三行，不在主内容流中。.wb-pws-terminal-drawer {  grid-area: terminal;  min-width: 0;  min-height: 0;  height: 220px;  overflow: hidden;  border-top: 1px solid #DCEBFA;  background: rgba(255,255,255,.86);}.wb-pws-terminal-drawer.is-collapsed {  height: 42px;}.wb-pws-terminal-drawer__body {  height: calc(100% - 40px);  min-height: 0;  overflow: auto;}Tab内容来源终端npm test / shell 输出现有 Shell 输出工具调用read_file、search_code、edit_file 等tool_operations测试输出测试通过/失败/跳过test output执行日志Agent 过程日志agent logs上下文记忆压缩快照、引用文件、恢复点context memory阶段 7 验收• 底部终端固定在页面底部。• 折叠/展开不影响三栏主体布局。• 终端内容只在 drawer body 内滚动。12. 阶段 8：响应式断点与宽屏适配排查报告显示当前主要断点是 1024px，1280/1440/1536 宽度仍受 max-width 影响。完成阶段 1 后，还需要明确桌面布局断点。@media (min-width: 1280px) {  .wb-pws-layout {    grid-template-columns: 300px 360px minmax(0, 1fr);    grid-template-rows: 48px minmax(0, 1fr) 220px;  }}@media (min-width: 1024px) and (max-width: 1279px) {  .wb-pws-layout {    grid-template-columns: 280px 340px minmax(420px, 1fr);    grid-template-rows: 48px minmax(0, 1fr) 210px;  }}@media (max-width: 1023px) {  .wb-pws-layout {    grid-template-columns: 1fr;    grid-template-rows: 48px minmax(0, 1fr) 180px;    grid-template-areas:      "topbar"      "agent"      "terminal";  }  .wb-pws-project-col,  .wb-pws-code-col {    display: none; /* 后续改为 drawer */  }}窗口宽度预期布局验收重点1536px完整三栏 + 底部终端不得出现居中卡片1440px完整三栏 + 底部终端Code 区宽度充足1280px完整三栏，Code 区可稍窄三栏仍可见1024–1279px紧凑三栏或 Code 可折叠不回退为长卡片<1024px单列/抽屉功能入口保留阶段 8 验收• 1280px、1440px、1536px 均展示桌面多栏。• 不会在宽屏下使用居中卡片模式。• <1024px 才允许单列或抽屉。13. 阶段 9：空任务状态与数据加载验证排查报告显示空任务不会提前 return 旧空页，这是符合目标的。修复过程中必须保持这一点。场景预期表现禁止表现项目无任务保留三栏骨架，Agent 区显示“暂无任务”，Code 区显示 empty 占位return 整页空状态未配置代码目录Code 区显示“请设置代码目录”，终端保留隐藏整个 Code 列任务执行中Agent Timeline 更新，Code 区展示变更，终端有日志全页刷新或跳转旧页面任务失败显示失败节点、错误日志、重试按钮仅弹 toast 无记录阶段 9 验收• 无任务、无代码目录、执行中、失败四种状态均不破坏骨架。• 数据为空只影响区域内容，不影响整体布局。14. 阶段 10：验收测试与回归检查完成修复后，请 Cursor 输出自检报告，不要只回复“已完成”。验收项通过标准检查方式计算样式#wbProjectWorkspace max-width none，margin 0，width 100%DevTools computed style布局方式.wb-pws-layout display:gridDevTools / 控制台列结构project / agent / code 三列可见截图终端固定底部 Grid 行，可折叠交互测试滚动主页面不滚动，各面板内部滚动长任务/长 diff 测试任务空态骨架保留新建空项目测试宽屏1280/1440/1536 都是多栏窗口缩放截图旧功能项目选择、任务、Diff、终端、审批不报错功能回归15. Cursor 分批执行提示词以下提示词建议分批发送给 Cursor，避免一次性重构失控。15.1 第 1 批：只解除 CSS 限宽冲突请只修复项目工作区被居中限宽的问题，不要改 JS 逻辑。重点检查 ui-nebula-theme.css、project-workspace-codex.css、workbench-dev.css。目标：当 body 有 jl-project-workspace-active 时，#panel-ai #wbProjectWorkspace 必须 width:100%、height:100%、max-width:none、margin:0、padding:0、overflow:hidden。禁止继续保留 max-width:960px、max-width:min(1400px...)、margin:0 auto、居中白卡片样式。完成后输出：修改文件、删除/覆盖的规则、#wbProjectWorkspace 的 computed style。15.2 第 2 批：把布局从 Flex 改成 Grid请在 projectWorkspaceLayout.js 与 project-workspace-codex.css 中，将项目工作区主骨架从两栏 Flex 改成三栏 Grid + 底部 TerminalDrawer。Grid 区域为 topbar、project、agent、code、terminal。桌面端 grid-template-columns: 300px 360px minmax(0,1fr);grid-template-rows: 48px minmax(0,1fr) 220px。保留现有业务挂载点，例如 wbPwsCodeMount、wbPwsTerminalDrawer。完成后输出 DOM 结构和 .wb-pws-layout computed display/grid-template。15.3 第 3 批：拆出项目任务侧栏与 Agent 面板滚动请将项目与任务列表放入独立 .wb-pws-project-col，不要继续挤在 Agent 列里。Agent 列改成 header + scroll body + composer 三段结构。.wb-pws-agent-col 本身不能 overflow:auto，只允许 .wb-pws-agent-scroll 滚动。输入框固定在 Agent 区底部。完成后输出：项目任务栏 DOM、Agent 子结构、允许滚动的容器清单。15.4 第 4 批：重组 CodeReviewPanel 与 TerminalDrawer请把右侧 CodeReviewPanel 调整为以 Diff 审查为核心：顶部 Tabs、变更概览、Diff 主体、底部操作栏。Shell、工具调用、测试输出、执行日志不要继续纵向堆在代码区内容流中，应统一放入底部 TerminalDrawer 的 Tab 中。TerminalDrawer 必须固定在 Grid 的 terminal 区域，折叠/展开不能影响页面整体滚动。完成后输出：Code 区结构、TerminalDrawer 结构、功能回归结果。15.5 第 5 批：响应式与最终验收请修复响应式断点：>=1280px 使用完整三栏桌面布局，1024-1279px 使用紧凑三栏或可折叠 Code 区，<1024px 才允许单列/抽屉。请分别在 1280px、1440px、1536px 下自检，不允许出现居中卡片或单列长页面。完成后输出最终验收报告，包含截图描述、computed style、grid-template、滚动容器、空任务状态、功能回归结果。16. 附录：关键 CSS / DOM 参考片段16.1 最小可用 Grid 样式body.jl-project-workspace-active #panel-ai,body.jl-project-workspace-active #wbProjectWorkspace {  width: 100%;  height: 100%;  overflow: hidden;}.wb-pws-layout {  display: grid;  grid-template-columns: 300px 360px minmax(0, 1fr);  grid-template-rows: 48px minmax(0, 1fr) 220px;  grid-template-areas:    "topbar topbar topbar"    "project agent code"    "terminal terminal terminal";  width: 100%;  height: 100%;  min-width: 0;  min-height: 0;  overflow: hidden;}16.2 允许滚动的容器白名单允许 overflow-y:auto / overflow:auto 的容器：- .wb-pws-project-list- .wb-pws-task-list- .wb-pws-agent-scroll- .wb-pws-code-body- .wb-pws-terminal-drawer__body禁止滚动的容器：- body- .jl-app-shell- #panel-ai- #wbProjectWorkspace- .wb-pws-layout- .wb-pws-agent-col- .wb-pws-code-col16.3 最终整改完成定义用户打开项目工作区后，第一眼看到的是全屏浅蓝白开发工作台，而不是中间白色长卡片。项目任务栏、Agent 执行区、代码 Diff 审查区、底部终端抽屉同时可见。页面没有全局滚动条；长内容只在对应面板内部滚动。空任务、无代码目录、执行中、失败、审批等状态均不会破坏页面骨架。1280px、1440px、1536px 均保持桌面多栏布局。视觉色调保持当前项目的浅蓝白，不使用黑色主题。