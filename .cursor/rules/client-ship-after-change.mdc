---
description: 桌面客户端迭代后必须归档到「最新客户端」并执行安装程序
alwaysApply: true
---

# 客户端发布（写死）

在修改了 **Electron 桌面客户端** 相关交付物（`main.js`、`preload.js`、`index.html`、与安装/打包相关的 `package.json` 配置等）并准备交付时，**必须**在项目根执行：

```bash
npm run ship:latest-client
```

该命令会：

1. 执行 `npm run build` 生成 Windows 安装包与便携包；
2. 将产物复制到项目根目录下的 **`最新客户端`**（含带时间戳的历史子目录与按版本号覆盖的扁平目录），并写入 `release-history.jsonl`；
3. **运行 NSIS 静默安装**，将最新版安装到本机用户目录。

**不要**在交付流程中设置 `SKIP_INSTALL=1`。若仅需装完不自动启动应用，可使用 `SKIP_LAUNCH_AFTER_INSTALL=1 npm run ship:latest-client`。

`npm run install:client` 与 `npm run ship:latest-client` 等价，均走上述固定流程。

若仅需打包而不归档、不安装，可使用 `npm run build` 或带 `SKIP_INSTALL=1` 的 `npm run release:latest-client`（仅限本地调试，不作为迭代交付的默认步骤）。
